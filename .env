# Server configuration
PORT=3000

# Frontend URL (change this to your actual frontend URL in production)
# Use * during development to allow requests from any origin
FRONTEND_URL=*

# Cache TTL in seconds (24 hours)
CACHE_TTL=86400